# AGENT_BRIEF — ripgrep

> Forensic brief for AI coding agents. **Read this first.**
> Each rule carries a confidence tag (DEC-015): `[EXTRACTED]` from AST/git, `[INFERRED]` from a ranking or heuristic.
> Full detail: `MAP.md`, `HOTPATHS.md`, `ARCHAEOLOGY.md`, `MENTAL_MODEL.md`.

## What this is

A **Rust** codebase, 84 source file(s), 15 test file(s), 50 contributor(s), ~10.2 years old.

## Rules

### Always

- Treat `parse_low_raw` as the most-called symbol (545 inbound calls per the DEC-025 resolver) — signature changes touch every caller `[INFERRED]`
- When you touch `crates/printer/src/standard.rs`, also check `crates/printer/src/util.rs` — they co-change in 14 shared commit(s) per the DEC-027 join `[INFERRED]`

### Never

- Never assume `Cargo.lock` is stable — it is the repo's biggest churn point (476 commits) `[EXTRACTED]`

## Most central files

1. `crates/printer/src/util.rs`
2. `crates/ignore/src/pathutil.rs`
3. `crates/searcher/src/sink.rs`
4. `crates/globset/src/pathutil.rs`
5. `crates/printer/src/json.rs`

## Where things live

- `crates` — 74 file(s)
- `(repo root)` — 1 file(s)

---

*Generated by forensic-deepdive 0.3.0 on 2026-05-30. Regenerate with `forensic update` — do not hand-edit.*
