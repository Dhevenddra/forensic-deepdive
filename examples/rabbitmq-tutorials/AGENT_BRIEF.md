# AGENT_BRIEF — python

> Forensic brief for AI coding agents. **Read this first.**
> Each rule carries a confidence tag (DEC-015): `[EXTRACTED]` from AST/git, `[INFERRED]` from a ranking or heuristic.
> Full detail: `MAP.md`, `HOTPATHS.md`, `ARCHAEOLOGY.md`, `MENTAL_MODEL.md`.

## What this is

A **Python** codebase, 12 source file(s), 1 contributor(s), under a day old.

## Rules

### Always

- Treat `fib` as the most-called symbol (3 inbound calls per the DEC-025 resolver) — signature changes touch every caller `[INFERRED]`
- This repo is cross-stack — `FibonacciRpcClient.call` calls backend `<module>` over `queue::rpc_queue` (+4 more — see HOTPATHS `## Cross-stack routes`) `[EXTRACTED]`

### Never

- Never assume `.devcontainer/Dockerfile` is stable — it is the repo's biggest churn point (1 commits) `[EXTRACTED]`

## Most central files

1. `emit_log.py`
2. `emit_log_direct.py`
3. `emit_log_topic.py`
4. `new_task.py`
5. `receive.py`

## Where things live

- `(repo root)` — 12 file(s)

---

*Generated by forensic-deepdive 0.6.0 on 2026-06-12. Regenerate with `forensic update` — do not hand-edit.*
