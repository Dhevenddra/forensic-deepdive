# MENTAL_MODEL — python

> How to think about this codebase. v0.1 emits a deterministic skeleton from structure and history; v0.2 will enrich it with LLM synthesis.
> **Confidence:** facts are `EXTRACTED` (deterministic from AST and git) unless a section / line says otherwise (DEC-015).

## At a glance

- A **Python** codebase of 12 source file(s).
- 1 contributor(s) over under a day.

## Likely entry points

_Confidence: `INFERRED` (DEC-015)._

_No obvious entry-point file names detected._

## Core modules

_Confidence: `INFERRED` (DEC-015)._

The load-bearing files — highest dependency centrality (PageRank over the symbol graph):

1. `emit_log.py` (centrality 0.0833)
2. `emit_log_direct.py` (centrality 0.0833)
3. `emit_log_topic.py` (centrality 0.0833)
4. `new_task.py` (centrality 0.0833)
5. `receive.py` (centrality 0.0833)
6. `receive_logs.py` (centrality 0.0833)
7. `receive_logs_direct.py` (centrality 0.0833)
8. `receive_logs_topic.py` (centrality 0.0833)

## Layers

Top-level directories by analyzed-file count:

| Directory | Files |
| --- | --- |
| `(repo root)` | 12 |

---

*Generated by forensic-deepdive 0.6.0 on 2026-06-12. Regenerate with `forensic update` — do not hand-edit.*
