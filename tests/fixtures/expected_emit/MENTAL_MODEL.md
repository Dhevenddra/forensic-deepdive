# MENTAL_MODEL — tiny_fixture

> How to think about this codebase. v0.1 emits a deterministic skeleton from structure and history; v0.2 will enrich it with LLM synthesis.
> **Confidence:** facts are `EXTRACTED` (deterministic from AST and git) unless a section / line says otherwise.

## At a glance

- A **Python** codebase of 12 source file(s).

## Likely entry points

_Confidence: `INFERRED`._

Files whose names conventionally mark an entry point (stem matches `main` / `app` / `cli` / `index` / `__main__` / `server` / `run` / `manage`):

- `mobile/lib/main.dart`
- `src/blog/cli.py`

## Core modules

_Confidence: `INFERRED`._

The load-bearing files — highest dependency centrality (PageRank over the symbol graph):

1. `mobile/lib/post.dart` (centrality 0.1735)
2. `src/blog/utils.py` (centrality 0.1575)
3. `mobile/lib/api.dart` (centrality 0.1117)
4. `src/blog/models.py` (centrality 0.0883)
5. `src/blog/config.py` (centrality 0.0814)
6. `src/blog/render.py` (centrality 0.0814)
7. `mobile/lib/feed_view.dart` (centrality 0.0746)
8. `src/blog/feed.py` (centrality 0.0635)

## Layers

Top-level directories by analyzed-file count:

| Directory | Files |
| --- | --- |
| `src` | 7 |
| `mobile` | 4 |

---

*Generated by forensic-deepdive 0.8.0 on 2026-05-23. Regenerate with `forensic update` — do not hand-edit.*
