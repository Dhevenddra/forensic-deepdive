---
description: Forensic codebase brief for AI agents
alwaysApply: true
---

# Codebase brief for AI agents

This repository has a forensic deep-dive. **Read `docs/codebase/AGENT_BRIEF.md` first** — it is the headline brief for AI coding agents.

Deeper detail lives alongside it: `MAP.md`, `HOTPATHS.md`, `ARCHAEOLOGY.md`, `MENTAL_MODEL.md`.

Five single-intent skills under `.claude/skills/codebase-*/` tell Claude Code which artifact to consult for which question. An MCP server (`forensic serve`) exposes nine composite tools (`impact`, `context`, `archaeology`, `flow`, `query`, `record_insight`, `recall_insights`, `visualize`, `trace`) for agents that prefer programmatic queries.

_Generated by forensic-deepdive — regenerate with `forensic update`. Delete this file to opt out._
